# Database
DB_PASSWORD=postgres

# Auth (hardcoded credentials)
AUTH_USERNAME=admin
AUTH_PASSWORD=admin

# JWT
JWT_SECRET_KEY=change-me-in-production-use-openssl-rand-base64-32

# Domain (for certbot - prod only)
# DOMAIN=monitocam.example.com
# CERTBOT_EMAIL=admin@example.com
