[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "monitocam"
version = "1.0.0"
description = "IP Camera Recording System"
requires-python = ">=3.9"
