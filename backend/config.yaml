app:
  host: "0.0.0.0"
  port: 8585
  debug: false

storage:
  base_path: "/mnt/nas-pictures/monitocam"
  nas_path: "/mnt/nas-pictures/monitocam"
  use_nas: true

recording:
  fragment_duration: 1800  # 30 minutes
  gif_duration: 900  # seconds
  gif_fps: 2
  gif_speed: 15  # playback speed multiplier (4x = covers 20s of video in 5s GIF)

jwt:
  secret_key: "change-this-in-production"
  algorithm: "HS256"
  expire_minutes: 1440  # 24 hours
