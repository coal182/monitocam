app:
  host: "0.0.0.0"
  port: 8585
  debug: false

storage:
  base_path: "/recordings"

recording:
  fragment_duration: 1800  # 30 minutes
  gif_duration: 900  # seconds
  gif_fps: 12
  gif_speed: 30  # playback speed multiplier (4x = covers 20s of video in 5s GIF)

jwt:
  secret_key: "change-this-in-production"
  algorithm: "HS256"
  expire_minutes: 1440  # 24 hours
